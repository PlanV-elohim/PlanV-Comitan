# Plan B Ministerio Heloim

This project is a web application for Ministerio Heloim.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set up your environment variables in `.env.local`
3. Run the app:
   `npm run dev`
