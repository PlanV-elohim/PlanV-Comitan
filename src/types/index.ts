export interface CampEvent {
    id: number;
    date: Date;
    endDate: Date;
    title: string;
    description: string;
    location: string;
    time: string;
    spots: number;
    available: number;
    image: string;
}
